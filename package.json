{
  "name": "shieldscore",
  "version": "1.0.0",
  "description": "ShieldScore — Private Credit Passport on Midnight Network",
  "private": true,
  "workspaces": [
    "contract",
    "frontend"
  ],
  "scripts": {
    "contract:compile": "npm run compile --prefix contract",
    "contract:test": "npm run test --prefix contract",
    "contract:deploy": "npm run deploy --prefix contract",
    "contract:check-balance": "npm run check-balance --prefix contract",
    "frontend:dev": "npm run dev --prefix frontend",
    "frontend:build": "npm run build --prefix frontend",
    "test": "npm run contract:test",
    "build": "npm run frontend:build",
    "dev": "npm run frontend:dev"
  },
  "keywords": [
    "midnight",
    "zero-knowledge",
    "compact",
    "privacy",
    "credit-score",
    "defi",
    "confidential-credentials"
  ],
  "license": "Apache-2.0"
}
